# v219：票根收整、場景前後排序與眨眼修正

## 本次更新

- 訂單票根減少上下留白，操作連結與後台單號放在同一個底列。歷史訂單維持細底線「再點一次」；手機窄版自動換行，日期標題不拆字。
- 吧台、椅子與角色使用同一套腳底深度排序，角色到達前緣時會顯示在物件前方，修正只露出腳與姓名的臨界位置。
- 修正小白、攤販與布告欄的排序基準；走到小白前面會蓋住小白，後面仍受到遮擋。
- 吧台碰撞區由長邊及右側回角組成 L 型；保留吧台右端與櫃子之間的通道，可從後方繞回前方。
- 眨眼只作用於顯示畫布，不再寫入走路影格快取；移動時不觸發眨眼，停止時仍可自然眨眼。
- 原畫素材、前大後小、行走與待機動作參數、付款與訂單資料處理維持原樣。

## 驗證

- `node tests/scene-v218.cjs`：內嵌腳本語法、64 組路徑、116 段可走線段、8 組逐幀抵達、方向鍵碰撞、深度比例；新增圖層臨界與右側 L 型通道斷言。
- `node tests/blink-v219.cjs`：眨眼途中開始走路，80 次步伐輪播不再帶入閉眼快取，停止後正常眨眼。
- 隔離瀏覽器重現吧台腳底 y=430 的遮擋問題，修正後完整顯示；椅子前 y=456 完整顯示。
- 實走吧台後方 (1100,305) 到右側前方 (1350,460)，沿右側縫隙抵達且腳底未進入障礙區。
- 小白前方 (190,650) 與後方 (190,580) 截圖確認前後遮擋。
- 桌機確認進行中及歷史票根；一般票根約 200–233px 高。390px 窄版確認日期、協助處理訊息、再點一次與單號無橫向溢出。
- 測試頁使用假資料並隔離 Firebase；未執行付款或取消訂單。

## 上線後手測

1. 重新整理大廳，左下版本應為 v219；訂單票根留白縮小，底部操作與單號並排。
2. 行走時眼睛不應隨每一步反覆開閉；停止時仍保有偶爾眨眼。
3. 沿吧台正面與椅子前方走，人物應完整顯示；往後走則受物件遮擋。
4. 從吧台後方向右走，沿 L 型桌角與櫃子之間的通道走回大廳。
5. 走到小白前面與後面，確認角色與小白的前後順序隨位置改變。
