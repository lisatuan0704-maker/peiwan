# v214 繪師大廳與清透功能介面

## 內容
- 更換正式大廳背景，使用繪師 1600 × 900 原始 PNG；保留程式角色，校正門口、燈籠及布告欄互動位置。
- 整合已定案的清透漸層、深色標題塊、空心文字、分區幾何動態。成就、錢包、訂單、信箱、設定和點單各自保有代表色與不同版型。
- 左上保留原人物畫布、動態頭像框、VIP 與 Discord；點擊頭像仍開啟書本個人頁與頭像框頁。手機直式共用同一個頭像節點。
- 設定保留右上唯一入口；右下保留好友、成就、錢包、信箱、訂單、時裝間、背包及主要點單入口。
- 訂單票根沿用正式訂單、付款及取消狀態。透過既有 workOrderId 唯讀取得後台 no；未建立時顯示待建立／同步。條碼為視覺識別裝飾，文字單號為實際查單依據。
- 錢包使用原餘額與後台儲值方案，保留原交易紀錄與付款處理器；新增通往原優惠券收藏頁的連結。
- 加入獨立後髮、前髮待機擺動，單層最大偏移小於原圖一個像素。沿用原呼吸和行走動作，保留原五官及造型資料；輕量模式和減少動態設定會停用新增動態。

## 驗證
- 本機隔離頁停用 Firebase，使用獨立測試資料；未執行正式付款、儲值、退款或建立正式訂單。
- 檢查桌面 1280 × 720、手機 390 × 844 及 320 × 640。
- 實際走查頭像 → 書本 → 頭像框、六種功能視窗、點單 → 既有新版名簿。
- 程式檢查覆蓋訂單狀態、退款狀態文字、自助取消入口條件、文字跳脫、後台單號讀取與快取、保留付款處理器及繪師原檔位元組一致性。
- 既有時裝與名簿接線測試通過，包含原商品價格、持有權限與穿戴保存。
- 動態檢查覆蓋資料不變、行走恢復原姿勢、重繪節流、輕量與減少動態。實際繪製確認髮層產生像素變化。

## 回復參考
上一個正式版本為 24b1188。此次未修改 Firebase 規則、後台訂單程式、原付款及退款處理器。
