# v218：場景分層、票根文字操作與數字字型

## 本次更新

- 使用繪師提供的 1600 × 900 PNG 原始檔，基底、吧台／攤販、椅子、店貓、食物及三個小人偶依原座標顯示。
- 同一張「店鋪・花・吧台」以 CSS 顯示裁切分出遮擋順序，沒有改畫、縮裁或重存素材。
- 吧台兩端可繞入後方走道，後方可露頭左右移動；左側攤販可走到後方並受到棚架遮擋。
- 新增腳底碰撞與繞路計算，方向鍵沿障礙邊緣移動；既有桌機 WASD／方向鍵操作保持原樣。
- 角色大小按世界深度調整，並以角色像素腳底對齊地面；原 v217 的行走、待機與轉身動作參數保持相同。
- 四個座位互動位置對齊繪師的四張椅子。
- 小紫、小白、小花按原圖層位置加入。枕頭使用「放偶的枕頭跟小狼」，另一張花朵枕頭原檔保留作為替代版，不重複疊放。
- 訂單「再點一次」改為細底線文字與箭頭，仍呼叫原本的 `reorderPlay()`。
- 依 `大廳視窗試做.html` 確認數字字型為 Lilita One，沿用專案現有本機字型檔。主要價格、金幣、票根日期與部分收藏序號採用同款數字；中文和後台單號保持原字體。

## 驗證

- `node tests/scene-v218.cjs`：HTML 內嵌腳本語法、64 組路徑與 116 段可行走線段、8 組逐幀抵達、方向鍵碰撞、前後大小關係。
- 隔離測試頁不連 Firebase：實走吧台後左、後右、攤販後與攤販前，皆抵達且沒有腳底進入碰撞區。
- 瀏覽器確認吧台後露臉、攤販遮擋、小紫／小花／小白原位、切換森林後隱藏全部酒館圖層。
- 票根「再點一次」顯示與點擊進入原點單流程；390px 寬度下操作列未溢出。
- 與上一版逐字比較原版行走／待機動作、變形參數及位置同步寫入區塊；資料與付款處理函式未修改。

## 上線後手測

1. 開啟 `lobby.html?v=218`，確認左下版本為 v218，三個小人偶與店貓各在原圖位置。
2. 用 WASD／方向鍵從吧台左端或右端繞上去，往後走到走道上緣，應露出頭部並可左右移動。
3. 從左側攤販右側走向棚架後，再沿右側繞回前方；棚架會遮擋角色。
4. 查看歷史訂單：「再點一次 ↗」應為細底線文字，價格及日期使用圓胖數字，點擊仍進入點單流程。
5. 切換森林再回酒館，場景圖層應正確切換，角色前大後小與原有待機動作仍在。
