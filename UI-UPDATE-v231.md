# v231 玩家位置同步

- 其他玩家由本人回報世界座標；觀看者不再重新找路或用較慢的速度追趕。
- 初次收到位置、切換地圖或大幅落差立即對齊；一般走動以 120ms 平滑顯示。
- 新版座標已在本人端完成碰撞，觀看者不再二次移位；舊版座標仍保留門牆位置修正。
- 改為約 150ms 回報有變動的位置；停止立即補送最後位置，包含方向、坐下與移動狀態。
- 寫入成功才記住已送位置，失敗 1 秒後重試，斷線重連重新送出；定期維持位置狀態。
- 切換角色時清除舊角色的本人標記；只有目前角色能回報自己的位置，避免相互覆寫。

## 驗證

- `node tests/presence-v231.cjs`：首次對齊、跨圖、120ms 收斂、無效座標、舊版相容、網路失敗重試及停止回報。
- 100ms 模擬網路延遲下重播繞吧台路徑，最大移動誤差 47.5 個世界像素，停止後精確對齊。
- 隔離瀏覽器使用本人與朋友兩個獨立頁面，經本機位置服務送出與接收；驗證首次進場、移動、停止、重進及切換地圖。
- 保留 v229 場景碰撞、註冊造型與 v230 染髮功能。
