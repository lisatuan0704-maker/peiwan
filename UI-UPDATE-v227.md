# v227 名簿角色位置調整工具

新增獨立頁面 `tavern-ui/portrait-adjust.html`，可在正式名簿樣式中分別調整卡布、MOMO、縭的圖片位置與大小。

- 支援拖曳、滑桿、數字欄位、方向鍵微調，以及單位／全部還原。
- 提供 900、760、600、390px 預覽寬度。
- 設定僅暫存於目前瀏覽器，可複製或下載 JSON；不寫入正式角色設定或資料庫。
- JSON 使用圖片中心相對於名簿列的百分比座標，以及圖片高度相對列高的百分比。套用時圖片使用 `translate(-50%,-50%)`。
- 圖片沿用原始繪師 PNG，預設構圖換算自 v226 名簿。

驗證：JavaScript 語法檢查通過；實際操作 MOMO 的數值、縮放、拖曳、重載保留設定、390px 預覽、單位還原及剪貼簿匯出，確認其他角色設定不受影響。
