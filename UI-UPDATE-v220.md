# v220：櫃體碰撞與右側通道修正

- 櫃子碰撞恢復到原圖櫃體左緣，不再把櫃子的左半部當成通道。
- 吧台採用 L 型多邊形，右端邊界沿透視斜線收合；可通行路線繞過櫃子左側及前緣。
- 線段碰撞檢查納入斜邊，避免大步幅穿越桌角。
- 中途轉角只有在通往下一段的路線無阻擋時才提前轉向；未完成轉角前不會誤判抵達而停住。
- 舊版若保存了櫃體內位置，載入後會由既有位置校正回到可行走地板。
- 卡布腳下枕頭恢復到攤販陰影與告示牌上方、卡布下方，不再被遮掉半片；素材本身維持原檔。

## 驗證

- `node tests/scene-v218.cjs`：64 組路徑、140 段線段、8 組逐幀抵達、方向鍵碰撞、前後比例及 HTML 內嵌語法。
- `node tests/cabinet-v220.cjs`：櫃體網格阻擋、三側連續推進、右側雙向通道逐段及逐幀驗證。
- `node tests/blink-v219.cjs`：走路影格與眨眼快取隔離回歸。
- 隔離瀏覽器確認舊位置 (1318,330) 校正為地板 (1318,380)，沒有再站進櫃子。
- 隔離瀏覽器實走吧台後方到右側前方，腳底全程處於可行走區。
- 隔離瀏覽器確認反向抵達吧台後方，以及卡布腳下兩個枕頭完整顯示。

## 手測

1. 重新整理大廳，確認左下版本 v220。
2. 從櫃子前方往上走，人物應停在櫃前地板。
3. 從吧台後方沿右側斜邊繞出，再反向走回；兩個方向皆應通行。
